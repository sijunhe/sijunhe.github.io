<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: kaggle | Blog | Sijun He]]></title>
  <link href="http://sijunhe.github.io/blog/categories/kaggle/atom.xml" rel="self"/>
  <link href="http://sijunhe.github.io/"/>
  <updated>2017-02-08T22:25:00-08:00</updated>
  <id>http://sijunhe.github.io/</id>
  <author>
    <name><![CDATA[Sijun He]]></name>
    <email><![CDATA[sijun.he@hotmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[San Francisco Crime Visualization]]></title>
    <link href="http://sijunhe.github.io/blog/2015/12/15/san-francisco-crime-visualization/"/>
    <updated>2015-12-15T15:42:11-08:00</updated>
    <id>http://sijunhe.github.io/blog/2015/12/15/san-francisco-crime-visualization</id>
    <content type="html"><![CDATA[<p>The purpose of this post is two-folded</p>

<ul>
<li>To complete the crime analytics visualization assignment from <a href="https://www.coursera.org/learn/data-results">Coursera: Communicating Data Science Results</a></li>
<li>To try my hands at data visualization with ggplot2 and ggmap.</li>
</ul>


<!--more-->


<p>The dataset given by Coursera is actually a portion of the dataset from <a href="https://www.kaggle.com/c/sf-crime">Kaggle: San Francisco Crime Challenge</a> so maybe I&rsquo;ll look into the Kaggle competition later as well.</p>

<h3>Problem Statement</h3>

<blockquote><p>In this assignment, you will analyze criminal incident data from Seattle or San Francisco to visualize patterns and, if desired, contrast and compare patterns across the two cities. You will produce a blog-post-style visual narrative consisting of a series of visualizations interspersed with sufficient descriptive text to make a convincing argument. You will use real crime data from Summer 2014 one or both of two US cities: Seattle and/or San Francisco.</p></blockquote>

<h3>Visualization</h3>

<p>For the Coursera assignment, I will produce a map of San Francisco with the top committed crimes and also look into the neighborhoods susceptible to particular kinds of crimes.</p>

<pre><code class="r">library(dplyr)
library(ggmap)
library(ggplot2)
data &lt;- read.csv("../datasci_course_materials/assignment6/sanfrancisco_incidents_summer_2014.csv")
data &lt;- subset(data, select = c(IncidntNum, Category, X, Y))
##getting ggmap basemap from Google Map
map &lt;- get_map(location = c(lon = -122.4367, lat = 37.7733), zoom = 13, maptype = "roadmap", color = "bw")
</code></pre>

<p>After importing the dataset, we will take a subset of the data since only the crime categories and the crime coordinates are needed for this visualization. For the visualization, we will use a popular R package called <strong>ggmap</strong>, which is designed for spatial visualization in combination with <strong>ggplot2</strong>. The map returned from the get_map command looks like this:</p>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post4/basemap.jpeg" alt="png" /></p>

<p>Then, we will start putting data points on the base map. However, before we do that, we need to take a look at the data first.</p>

<pre><code class="r">## use summarize command from dplyr package to group the crimes by their categories and do a number count
counts = summarize(group_by(data, Category), Counts=length(Category))
## descending order
counts = counts[order(-counts$Counts),]
top5 &lt;- data[data$Category %in% counts$Category[1:5],]
</code></pre>

<pre><code>          Category Counts
            (fctr)  (int)
1    LARCENY/THEFT   9466
2   OTHER OFFENSES   3567
3     NON-CRIMINAL   3023
4          ASSAULT   2882
5    VEHICLE THEFT   1966
6         WARRANTS   1782
7    DRUG/NARCOTIC   1345
8   SUSPICIOUS OCC   1300
9   MISSING PERSON   1266
10 SECONDARY CODES    442
..             ...    ...
</code></pre>

<p>It appears that theft is the most common crime in San Francisco, followed by &ldquo;other offenses&rdquo; and &ldquo;non-criminal&rdquo; crimes (not sure what those mean). Assault and Drug-related crimes are also quite common. Let&rsquo;s put these on the map and see how they distribute spatially.</p>

<pre><code class="r">Top5Crime &lt;- ggmap(map) + 
        geom_point(data = top5, aes(x=X, y=Y, colour=factor(Category)), alpha=0.10) + 
        ggtitle("Top 5 Crimes in San Francisco in Summer 2014") +
        theme_light(base_size=20) +
        guides(colour = guide_legend(override.aes = list(alpha=1.0, size=6.0), title="Type of Crime")) +
        scale_colour_brewer(type="qual",palette="Paired") +  
        theme(axis.line=element_blank(),
              axis.text.x=element_blank(),
              axis.text.y=element_blank(),
              axis.ticks=element_blank(),
              axis.title.x=element_blank(),
              axis.title.y=element_blank())
</code></pre>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post4/top5.jpeg" alt="png" /></p>

<p>Due to the clustering of the data points, the coloring doesn&rsquo;t work as well as expected but we can still get a good sense of how the crimes distributed spatially. The notorious Tenderloin neighborhood has the most concentrated crimes and that extends to the Downtown area north of it. The number of crimes declines significantly after crossing the Market St. to the south and the US101 to the west. The other interesting feature worth mentioning is that crimes decrease as we go east or north from the Tenderloin area into the Nob Hill and Financial District neighborhoods, but increase as we approach the water front.</p>

<p>Now let&rsquo;s take a particular look at Larceny/Theft, the most common crime in San Francisco. The spatial distribution of theft matches the top 5 crimes distribution quite well. Apart from the Tenderloin neighborhood, the areas with high theft counts are SOMA, Western Addition and Mission St.
<img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post4/larceny.jpeg" alt="png" /></p>

<p>Now we&rsquo;ll move away from the more common crimes and take a look at robbery, which is not among the top 10 most-committed crimes in San Francisco. While the count is still higher in Tenderloin neighborhood compared with other areas, robberies are actually pretty spread-out in the city.</p>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post4/robbery.jpeg" alt="png" /></p>

<p>For drug-related crimes, it is an entirely different story. The crimes related to drugs or narcotics are highly concentrated in the Tenderloin neighborhood north of Market St.and south of Geary St, while the distribution out of the Tenderloin area is quite sparse. I am guessing that drug-related crimes are usually organized by gangs, which tend to stay in certain area; robbery could be committed by any individual criminals, which makes it more spread out.</p>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post4/drug.jpeg" alt="png" /></p>

<p>So, the takeaway advice after reading the post is clearly to avoid the Tenderloin area when traveling to the city by the bar.</p>

<h3>Citation</h3>

<p>The R code I used was largely borrowed from Ben Hamner in his <a href="https://www.kaggle.com/benhamner/sf-crime/san-francisco-top-crimes-map">script</a> of San Francisco Top Crimes Map on Kaggle.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Kaggle| Titanic Part II]]></title>
    <link href="http://sijunhe.github.io/blog/2015/11/06/kaggle-titanic-part-ii/"/>
    <updated>2015-11-06T15:28:26-08:00</updated>
    <id>http://sijunhe.github.io/blog/2015/11/06/kaggle-titanic-part-ii</id>
    <content type="html"><![CDATA[<!--more-->


<pre><code class="python">import csv
import numpy as np
import pandas as pd
import pylab as plt
import re
from patsy import dmatrices
%matplotlib inline  
train = pd.read_csv('train.csv', header = 0)
test = pd.read_csv('test.csv', header = 0)
</code></pre>

<pre><code class="python">train.info()
</code></pre>

<pre><code>&lt;class 'pandas.core.frame.DataFrame'&gt;
Int64Index: 891 entries, 0 to 890
Data columns (total 12 columns):
PassengerId    891 non-null int64
Survived       891 non-null int64
Pclass         891 non-null int64
Name           891 non-null object
Sex            891 non-null object
Age            714 non-null float64
SibSp          891 non-null int64
Parch          891 non-null int64
Ticket         891 non-null object
Fare           891 non-null float64
Cabin          204 non-null object
Embarked       889 non-null object
dtypes: float64(2), int64(5), object(5)
memory usage: 90.5+ KB
</code></pre>

<pre><code class="python">test.info()
</code></pre>

<pre><code>&lt;class 'pandas.core.frame.DataFrame'&gt;
Int64Index: 418 entries, 0 to 417
Data columns (total 11 columns):
PassengerId    418 non-null int64
Pclass         418 non-null int64
Name           418 non-null object
Sex            418 non-null object
Age            332 non-null float64
SibSp          418 non-null int64
Parch          418 non-null int64
Ticket         418 non-null object
Fare           417 non-null float64
Cabin          91 non-null object
Embarked       418 non-null object
dtypes: float64(2), int64(4), object(5)
memory usage: 39.2+ KB
</code></pre>

<h3>Data Cleaning</h3>

<p>In last post, we have done some exploratory analysis of the data and also built two simple models, which gave us decent prediction accuracy. In this post, our question is, Can we do better?
We left off last time with Logistic Regression. And hopefully we just need to feed more predicators to the model for a better accuracy. However, before we can do that, we need to clean our data. We see that there are NAs in the Age, Fare, Cabin, and Embarked variables, so we will have to impute these values. For the Cabin variable, there are way too many NAs so imputing all those values would bring it an significant amount of noise. And the Cabin variable itself is hard to interpret anyways. So we will just impute the Age, Fare and Embarked variables.</p>

<pre><code class="python">def clean_data(df):
    # drop predicators that we are not going to use
    df.drop(['Ticket','Cabin'],inplace=True,axis=1)

    # encode the sex class to 0, 1
    df['Sex'] = df.Sex.map( {'female': 0, 'male': 1} ).astype(int)

    # simply fill the missing Embarked value with "S" since there are only 2 NAs
    df['Embarked'] = df.Embarked.fillna('S')

    # there is a 0 value in Fare column, which is unreasonable. Will have to replace it with NA first.
    df.Fare = df.Fare.map(lambda x: np.nan if x==0 else x)
    # build a pivot table to impute NAs with averaged Fare for all Pclass
    fare_pivot_table = df.pivot_table("Fare", index='Pclass', aggfunc='mean', dropna=True)
    # use pivot table to impute missing fare values
    df['Fare'] = df[['Fare', 'Pclass']].apply(lambda x: fare_pivot_table[x['Pclass']] if pd.isnull(x['Fare']) else x['Fare'], axis = 1)

    #build a pivot table to impute NAs with median Age according to Pclass and Sex
    age_pivot_table = df.pivot_table('Age', index=['Pclass','Sex'], aggfunc='median',dropna=True)
    df['Age'] = df[['Sex', 'Pclass', 'Age']].apply(lambda x: age_pivot_table[x.Pclass, x.Sex] if pd.isnull(x.Age) else x.Age, axis = 1)

    # define a age group categorical variable
    df['AgeGroup'] = 'adult'
    df.loc[ (df['Age']&lt;=10) ,'AgeGroup'] = 'child'
    df.loc[ (df['Age']&gt;=60) ,'AgeGroup'] = 'senior'

    # define a fare group categorical variable
    df['FareGroup'] = 'low'
    df.loc[ (df['Fare']&lt;=20) &amp; (df['Fare']&gt;10) ,'FareGroup'] = 'mid'
    df.loc[ (df['Fare']&lt;=30) &amp; (df['Fare']&gt;20) ,'FareGroup'] = 'mid-high'
    df.loc[ (df['Fare']&gt;30) ,'FareGroup'] = 'high'

    # Here we are adding interaction terms between predicators. I have included a few reasonable interaction terms in my mind 
    df['Family_Size']=train['SibSp']+train['Parch']+1

    # Generating the Title predicator
    df['Title'] = df['Name'].apply(lambda x: getTitle(x))
    df['Title'] = df.apply(getAndReplaceTitle, axis=1)

    return df

def getTitle(name):
    # use regex to match the titles in passenger names
    # titles are the characters preceded by a comma and a space (", ") and succeeded by a period (".") in a passenger's name.
    # Here we use the positive lookbehind assertion to find the preceding (", ") and the positive lookahead assertion to match the succeeding (".").
    m = re.search('(?&lt;=,\s)[a-zA-z\s]+(?=\.)', name)
    if m is None: 
        return np.nan
    else:
        return m.group(0)

def getAndReplaceTitle(df):
    # combine similar titles
    # the majority of the titles are pretty rare, which doesn't give much information about the passenger.
    title = df['Title']
    if title in ['Mr','Don', 'Major', 'Capt', 'Jonkheer', 'Rev', 'Col','Sir']:
        return 'Mr'
    elif title in ['the Countess', 'Mme','Mrs','Lady','Dona']:
        return 'Mrs'
    elif title in ['Mlle', 'Ms','Miss']:
        return 'Miss'
    elif title =='Dr':
            if df['Sex']=='Male':
                return 'Mr'
            else:
                return 'Mrs'
    else:
        return title
</code></pre>

<p>Not only did we impute the missing values, we also added in lots of additional predicators that could be correlated to the survival rate, since the correlation between survival rate and predicators might not be fully captured by the variables we had. Note that introducing new predicators, especaiily new categorical predicators, would increases the dimensionality of the data, which could potentially impact our prediction accuracy due to curse of dimensionality. Therefore we will use feature selection technique in our models</p>

<pre><code class="python">train = clean_data(train)
test = clean_data(test)
</code></pre>

<pre><code>/Library/Frameworks/Python.framework/Versions/2.7/lib/python2.7/site-packages/ipykernel/__main__.py:16: FutureWarning: scalar indexers for index type Int64Index should be integers and not floating point
</code></pre>

<pre><code class="python">#define a function in order to simplify future predictions and output
def predictAndOutput(estimator, output_filename):
        estimator.fit(train_data.values, train_response.values.ravel())
        output = estimator.predict(test_data).astype(int)
        submission = pd.DataFrame({"PassengerId": test["PassengerId"],"Survived": output})
        submission.to_csv(output_filename, index=False)
        return output
</code></pre>

<h2>Logistic Regression</h2>

<pre><code class="python">from sklearn.grid_search import GridSearchCV
from sklearn.linear_model import LogisticRegression
formula = 'Survived ~ C(Pclass) + Sex  + Age + SibSp + Parch + Family_Size + Fare + C(Embarked)+ C(Title) + C(AgeGroup)'
train_response,train_data = dmatrices(formula, data=train, return_type="dataframe")
test['Survived'] = 1 #insert a dummy survived variable in order to create dmatrices
test_response,test_data = dmatrices(formula, data=test, return_type="dataframe")

LRModel = LogisticRegression()
# Exhaustive search over all parameter combinations for the estimator that produce the best CV error
param_grid = {'penalty':['l1','l2'], 'C': [0.001, 0.01, 0.1, 1, 10, 100, 1000] }
GridSearch = GridSearchCV(LRModel, param_grid, cv = 10)
GridSearch.fit(train_data.values, train_response.values.ravel())
bestLRclf = GridSearch.best_estimator_
LRprediction = predictAndOutput(bestLRclf, "LROutput.csv")
</code></pre>

<p>We got <strong>0.77990</strong> this times, which is an great improvement over the 2 simple models we had in last post. Hooray, moving up the ladder! Let&rsquo;s take a look at the classifier that gave us the best results.</p>

<pre><code class="python">for i in range(1,len(train_data.columns.values)):
    print train_data.columns.values[i] , bestLRclf.coef_[0][i]
</code></pre>

<pre><code>C(Pclass)[T.2] -0.905477959537
C(Pclass)[T.3] -1.97896459743
C(Embarked)[T.Q] 0.0
C(Embarked)[T.S] -0.299162084187
C(Title)[T.Miss] -0.573336914082
C(Title)[T.Mr] -2.58797777339
C(Title)[T.Mrs] 0.0
C(AgeGroup)[T.child] 0.284813224479
C(AgeGroup)[T.senior] 0.0
Sex -0.78528900026
Age -0.025393089303
SibSp -0.49647079287
Parch -0.266397881617
Family_Size 0.0
Fare 0.00330362443408
</code></pre>

<p>We can tell from the coefficients of the predicators that some of the coefficients has been regularized to 0, which means that the predicator was not selected by the model. For the coefficients we can also observe the effect of the predicators. For example, passenger of Pclass 2 or 3would have a lower survival rate compared with passenger of Pclass 1 due to the negative coefficients.</p>

<h2>Random Forest</h2>

<p>Now we will move on to the more complicated Ensemble models, which usually offer the best out-of-box prediction accuracy. <a href="https://en.wikipedia.org/wiki/Random_forest">Random Forest</a> is one of the most commonly-used Ensemble method that is operated by constructing a random multitude of decision trees at training time and outputting the class that is the mode of the classes.</p>

<pre><code class="python">from sklearn.ensemble import RandomForestClassifier
RFModel = RandomForestClassifier(random_state=1, n_estimators=200)
param_grid = {'max_features':['sqrt',2, 5, 10, 15], 'min_samples_split':[4, 8, 10], 'min_samples_leaf':[2, 4, 6]}
GridSearch = GridSearchCV(RFModel, param_grid, cv = 10)
GridSearch.fit(train_data.values, train_response.values.ravel())
bestRFclf = GridSearch.best_estimator_
RFprediction = predictAndOutput(bestRFclf, "RFOutput.csv")
</code></pre>

<p>The most important parameters that we can tune for Random Forest is max_features, which is the number of features to consider when looking for the best split. Decreasing the max_features could improve randomness and reduce variance, while increasing the max_features would move the random forest more towards bagging. Min_samples_split and Min_samples_leaf are two stopping criteria of Random Forest that could contribute to overfitting. The GridSearchCV here is an exhaustive search over all parameter combinations for the estimator that produce the best 10-fold CV error.
The Random Forest Method achieved an accuracy of <strong>0.77990</strong> again.</p>

<pre><code class="python">indices = np.argsort(bestRFclf.feature_importances_)[::-1]
plt.figure()
plt.title("Feature importances")
plt.barh(range(len(train_data.columns.values)), bestRFclf.feature_importances_[indices], color="r")
plt.yticks(range(len(train_data.columns.values)), train_data.columns.values[indices])
plt.show()
</code></pre>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post2/plot1.png" alt="png" /></p>

<p>This plot illustrates the feature importantces generated from Random Forest. It is interesting that the dummy variable of Title Mr. is the most important feature, while Title Mrs. and Title Miss. are among the least influential features. The Age, Fare, Pclass and Sex variables also rank high in terms of importances, which is expected.</p>

<h2>Blend Models</h2>

<p>We have tried our hands at two models so far and they produced the exact same accuracy. So do both of the models make the same prediction on every test passengers? If not, we could somehow blend these two models together for a better result.</p>

<pre><code class="python"># Generating a list of test passenger indexes that have different predictions from the two models
different = list()
for i in range(len(RFprediction)):
    if RFprediction[i] != LRprediction[i]:
        different.append(i)
print different
print "Proportion of the predictions that the two models disagree on is " , len(different)/float(481)
</code></pre>

<pre><code>[1, 6, 19, 28, 32, 33, 34, 36, 37, 39, 41, 55, 63, 72, 86, 87, 88, 90, 94, 127, 132, 138, 144, 148, 158, 159, 169, 192, 199, 206, 249, 252, 268, 280, 291, 309, 313, 323, 344, 347, 367, 379, 382, 389, 390, 403, 409, 412, 417]
Proportion of the predictions that the two models disagree on is  0.101871101871
</code></pre>

<p>It does turn out that the two models didn&rsquo;t make the same prediction on every passengers, as the output shows that the models diagree on about 10% of prediction. This is quite desirable since we are very confident on the rest 90% of the predictions. Now we just needed to find a way to blend the two models together.</p>

<pre><code class="python">LRProba = bestLRclf.predict_proba(test_data)
print LRProba[different[1:5]]
</code></pre>

<pre><code>[[ 0.33960866  0.66039134]
 [ 0.29814563  0.70185437]
 [ 0.6590034   0.3409966 ]
 [ 0.53082693  0.46917307]]
</code></pre>

<p>For a ensemble method like Random Forest, the interpretbility is very low. However, the Logistic Regression method is actually very intrepretible, as we can compute the probabiliities of the binary responses for each predictions. Here we can see that for the 2nd prediction that the models differed on, the probabilities for 0 and 1 are 0.298 and 0.702, which means that the logistic regression was quite confident that this passengers was gonna survive, whereas for the 5th prediction that the models differed on, the probabilities for 0 and 1 are 0.530 and 0.469, the logistic regression was a lot less confident. Using the probabilities, we could derive an simple method to predict when the two models disagree.</p>

<pre><code class="python">from copy import deepcopy
CombinePrediction = deepcopy(RFprediction)
for i in range(len(different)):
    # If the probabilties of survival is greater than 0.7, predict survived
    if LRProba[different[i]][0] &gt; 0.7:
        CombinePrediction[different[i]] = 0
    # If the probabilties of survival is less than 0.3, predict perished   
    elif LRProba[different[i]][0] &lt; 0.3:
        CombinePrediction[different[i]] = 1
    else:
        continue
submission = pd.DataFrame({"PassengerId": test["PassengerId"],"Survived": CombinePrediction})
submission.to_csv("CombineOutput.csv", index=False)
</code></pre>

<p>This gives us <strong>0.79904</strong>, the best we have had so far and top 25% on the public leaderboard.</p>

<h2>Areas of Improvement</h2>

<p>Of course there is still a lot of room for improvement.
- Better tunning of parameters on Logistic Regression and Random Forest. Some people could use one single method to achieve 0.81 or above. Maybe include  <a href="http://scikit-learn.org/stable/modules/generated/sklearn.pipeline.Pipeline.html">Pipeline</a> in the CV process.
- Do a few models and combine them using majority vote. Maybe SVM, or Gradient Boosting.
- Better imputation on missing values
- Make use of familiy lastnames as a predicator.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Kaggle | Titanic Part I]]></title>
    <link href="http://sijunhe.github.io/blog/2015/10/31/kaggle-titanic-part-i/"/>
    <updated>2015-10-31T03:07:00-07:00</updated>
    <id>http://sijunhe.github.io/blog/2015/10/31/kaggle-titanic-part-i</id>
    <content type="html"><![CDATA[<!--more-->


<h2>Problem Statement</h2>

<blockquote><p>The sinking of the RMS Titanic is one of the most infamous shipwrecks in history.  On April 15, 1912, during her maiden voyage, the Titanic sank after colliding with an iceberg, killing 1502 out of 2224 passengers and crew. This sensational tragedy shocked the international community and led to better safety regulations for ships.<br/>
One of the reasons that the shipwreck led to such loss of life was that there were not enough lifeboats for the passengers and crew. Although there was some element of luck involved in surviving the sinking, some groups of people were more likely to survive than others, such as women, children, and the upper-class.<br/>
In this challenge, we ask you to complete the analysis of what sorts of people were likely to survive. In particular, we ask you to apply the tools of machine learning to predict which passengers survived the tragedy.</p>

<blockquote><p><a href="https://www.kaggle.com/c/titanic">Titanic: Machine Learning from Disaster</a></p></blockquote></blockquote>

<pre><code class="python">import csv
import numpy as np
import pandas as pd
import pylab as p
%matplotlib inline  
train = pd.read_csv('train.csv', header = 0)
test = pd.read_csv('test.csv', header = 0)
train.info()
</code></pre>

<pre><code>&lt;class 'pandas.core.frame.DataFrame'&gt;
Int64Index: 891 entries, 0 to 890
Data columns (total 12 columns):
PassengerId    891 non-null int64
Survived       891 non-null int64
Pclass         891 non-null int64
Name           891 non-null object
Sex            891 non-null object
Age            714 non-null float64
SibSp          891 non-null int64
Parch          891 non-null int64
Ticket         891 non-null object
Fare           891 non-null float64
Cabin          204 non-null object
Embarked       889 non-null object
dtypes: float64(2), int64(5), object(5)
memory usage: 90.5+ KB
</code></pre>

<p>From the info output, we could see that there were null values in the Age, Cabin and Embarked features. The majority of the values in Cabin feature were null and I felt the values were hard to interpret anyways so I decided to not include it in the model. There were also a few missing values in the Age and Embarked features, which would be imputed later.</p>

<pre><code class="python">train.head()
</code></pre>

<div>
<table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>PassengerId</th>
      <th>Survived</th>
      <th>Pclass</th>
      <th>Name</th>
      <th>Sex</th>
      <th>Age</th>
      <th>SibSp</th>
      <th>Parch</th>
      <th>Ticket</th>
      <th>Fare</th>
      <th>Cabin</th>
      <th>Embarked</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>0</th>
      <td>1</td>
      <td>0</td>
      <td>3</td>
      <td>Braund, Mr. Owen Harris</td>
      <td>male</td>
      <td>22</td>
      <td>1</td>
      <td>0</td>
      <td>A/5 21171</td>
      <td>7.2500</td>
      <td>NaN</td>
      <td>S</td>
    </tr>
    <tr>
      <th>1</th>
      <td>2</td>
      <td>1</td>
      <td>1</td>
      <td>Cumings, Mrs. John Bradley (Florence Briggs Th...</td>
      <td>female</td>
      <td>38</td>
      <td>1</td>
      <td>0</td>
      <td>PC 17599</td>
      <td>71.2833</td>
      <td>C85</td>
      <td>C</td>
    </tr>
    <tr>
      <th>2</th>
      <td>3</td>
      <td>1</td>
      <td>3</td>
      <td>Heikkinen, Miss. Laina</td>
      <td>female</td>
      <td>26</td>
      <td>0</td>
      <td>0</td>
      <td>STON/O2. 3101282</td>
      <td>7.9250</td>
      <td>NaN</td>
      <td>S</td>
    </tr>
    <tr>
      <th>3</th>
      <td>4</td>
      <td>1</td>
      <td>1</td>
      <td>Futrelle, Mrs. Jacques Heath (Lily May Peel)</td>
      <td>female</td>
      <td>35</td>
      <td>1</td>
      <td>0</td>
      <td>113803</td>
      <td>53.1000</td>
      <td>C123</td>
      <td>S</td>
    </tr>
    <tr>
      <th>4</th>
      <td>5</td>
      <td>0</td>
      <td>3</td>
      <td>Allen, Mr. William Henry</td>
      <td>male</td>
      <td>35</td>
      <td>0</td>
      <td>0</td>
      <td>373450</td>
      <td>8.0500</td>
      <td>NaN</td>
      <td>S</td>
    </tr>
  </tbody>
</table>
</div>


<pre><code class="python">train[train['Age'].isnull()].head()
</code></pre>

<div>
<table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>PassengerId</th>
      <th>Survived</th>
      <th>Pclass</th>
      <th>Name</th>
      <th>Sex</th>
      <th>Age</th>
      <th>SibSp</th>
      <th>Parch</th>
      <th>Ticket</th>
      <th>Fare</th>
      <th>Cabin</th>
      <th>Embarked</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>5</th>
      <td>6</td>
      <td>0</td>
      <td>3</td>
      <td>Moran, Mr. James</td>
      <td>male</td>
      <td>NaN</td>
      <td>0</td>
      <td>0</td>
      <td>330877</td>
      <td>8.4583</td>
      <td>NaN</td>
      <td>Q</td>
    </tr>
    <tr>
      <th>17</th>
      <td>18</td>
      <td>1</td>
      <td>2</td>
      <td>Williams, Mr. Charles Eugene</td>
      <td>male</td>
      <td>NaN</td>
      <td>0</td>
      <td>0</td>
      <td>244373</td>
      <td>13.0000</td>
      <td>NaN</td>
      <td>S</td>
    </tr>
    <tr>
      <th>19</th>
      <td>20</td>
      <td>1</td>
      <td>3</td>
      <td>Masselmani, Mrs. Fatima</td>
      <td>female</td>
      <td>NaN</td>
      <td>0</td>
      <td>0</td>
      <td>2649</td>
      <td>7.2250</td>
      <td>NaN</td>
      <td>C</td>
    </tr>
    <tr>
      <th>26</th>
      <td>27</td>
      <td>0</td>
      <td>3</td>
      <td>Emir, Mr. Farred Chehab</td>
      <td>male</td>
      <td>NaN</td>
      <td>0</td>
      <td>0</td>
      <td>2631</td>
      <td>7.2250</td>
      <td>NaN</td>
      <td>C</td>
    </tr>
    <tr>
      <th>28</th>
      <td>29</td>
      <td>1</td>
      <td>3</td>
      <td>O'Dwyer, Miss. Ellen "Nellie"</td>
      <td>female</td>
      <td>NaN</td>
      <td>0</td>
      <td>0</td>
      <td>330959</td>
      <td>7.8792</td>
      <td>NaN</td>
      <td>Q</td>
    </tr>
  </tbody>
</table>
</div>


<pre><code class="python">train[train['Embarked'].isnull()]
</code></pre>

<div>
<table border="1" class="dataframe">
  <thead>
    <tr style="text-align: right;">
      <th></th>
      <th>PassengerId</th>
      <th>Survived</th>
      <th>Pclass</th>
      <th>Name</th>
      <th>Sex</th>
      <th>Age</th>
      <th>SibSp</th>
      <th>Parch</th>
      <th>Ticket</th>
      <th>Fare</th>
      <th>Cabin</th>
      <th>Embarked</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th>61</th>
      <td>62</td>
      <td>1</td>
      <td>1</td>
      <td>Icard, Miss. Amelie</td>
      <td>female</td>
      <td>38</td>
      <td>0</td>
      <td>0</td>
      <td>113572</td>
      <td>80</td>
      <td>B28</td>
      <td>NaN</td>
    </tr>
    <tr>
      <th>829</th>
      <td>830</td>
      <td>1</td>
      <td>1</td>
      <td>Stone, Mrs. George Nelson (Martha Evelyn)</td>
      <td>female</td>
      <td>62</td>
      <td>0</td>
      <td>0</td>
      <td>113572</td>
      <td>80</td>
      <td>B28</td>
      <td>NaN</td>
    </tr>
  </tbody>
</table>
</div>


<p>So, let&rsquo;s dive deeper into the data and see if we could understand which features were more important for our model. The competition summary hinted that some groups of people were more likely to survive than others, such as women, children, and the upper-class. The obvious predicators in the data for women and children is sex and age. The Pclass predicator should be a direct representation of a passenger&rsquo;s socio-economic status. The Fare predicator might also indicates if a passenger was upper-class.</p>

<pre><code class="python">fig, axes = p.subplots(nrows=2, ncols=2, figsize=(15,10))
#compare male and female survival rate
male_female1 = pd.DataFrame({'Male':train[train['Sex'] == 'male']['Survived'].value_counts(),
                             'Female':train[train['Sex'] == 'female']['Survived'].value_counts()}, columns = ['Male','Female'])
male_female1.plot(kind = 'bar', title='Male and Female Survival Comparison 1', alpha = 0.3, stacked = True, ax=axes[0,0])

male_female2 = pd.DataFrame({'Survived':train[train['Survived'] == 1]['Sex'].value_counts(),
                             'Died':train[train['Survived'] == 0]['Sex'].value_counts()}, columns = ['Survived','Died'])
male_female2.plot(kind = 'bar', title='Male and Female Survival Comparison 2', alpha = 0.3, stacked = True, ax=axes[0,1])

#compare male and female survival rate across different age groups
male_survived_age = pd.DataFrame({'Survived':train[(train['Sex'] == 'male') &amp; (train['Survived'] == 1)]['Age'],
                                  'Died':train[(train['Sex'] == 'male') &amp; (train['Survived'] == 0)]['Age']}, columns = ['Survived','Died'])
male_survived_age.plot(kind = 'hist', title='Male Survival Age Histogram', alpha = 0.3, stacked = True, ax=axes[1,0])

female_survived_age = pd.DataFrame({'Survived':train[(train['Sex'] == 'female') &amp; (train['Survived'] == 1)]['Age'],
                                    'Died':train[(train['Sex'] == 'female') &amp; (train['Survived'] == 0)]['Age']}, columns = ['Survived','Died'])
female_survived_age.plot(kind = 'hist', title='Female Survival Age Histogram', alpha = 0.3, stacked = True, ax=axes[1,1])
</code></pre>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post1/plot1.png" alt="png" /></p>

<p>The first two plots showed that females had a significantly higher chance of survival compared with males, thanks to the &ldquo;women and children first&rdquo; maxim. Kudos to the gallant gentlemen who sacrificed themselves for the lives of the women and children. As we could see from the male survival histogram, male had a uniformly low survival rate across all age groups. It was noteworthy that the age group with the highest male survival rate was 0 - 10 years old, aka children, and elderly females almost all survived.</p>

<pre><code class="python">fig, axes = p.subplots(nrows=1, ncols=2, figsize=(15,5))
#compare survival rate of different socio-economic class
survival_pclass = pd.DataFrame({'Survived':train[train['Survived'] == 1]['Pclass'].value_counts(),
                    'Died':train[train['Survived'] == 0]['Pclass'].value_counts()}, columns = ['Survived','Died'])
survival_pclass.plot(kind = 'bar', title='Pclass Survival Comparison', alpha = 0.3, stacked = True, ax=axes[0])

#compare survival rate of different socio-economic class and sex
class_sex = pd.DataFrame({'High-Mid Class Male':train[(train['Sex'] == 'male') &amp; (train['Pclass'] != 1)]['Survived'].value_counts(),
                          'High-Mid Class Female':train[(train['Sex'] == 'female') &amp; (train['Pclass'] != 3)]['Survived'].value_counts(),
                          'Low Class Male':train[(train['Sex'] == 'male') &amp; (train['Pclass'] == 3)]['Survived'].value_counts(),
                         'Low Class Female':train[(train['Sex'] == 'female') &amp; (train['Pclass'] == 3)]['Survived'].value_counts()},
                         columns = ['High-Mid Class Male','High-Mid Class Female','Low Class Male','Low Class Female'])
class_sex.plot(kind = 'bar', title='Pclass Sex Survival Histogram', alpha = 0.3, ax=axes[1])
</code></pre>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post1/plot2.png" alt="png" /></p>

<p>Here I generated two plots to show that if the survival rate of passengers are related to their socio-economic class. From the plot on the left, it was evident that passengers with a Pclass of 3, which means lower-class, had a much lower survival rate compared with passengers with a Pclass of 1 or 2. The plot on the right shows how the a combination of Pclass and Sex variables could influence the survival rate of a passenger. It is notable that the different socio-economic status only seemed to significantly affect the survival rate of females, not males. The odds of a upper-class lady surviving the Titanic shipwreck is surprising high.</p>

<pre><code class="python">fig, axes = p.subplots(nrows=1, ncols=2, figsize=(15,5))
embarked_survival1 = pd.DataFrame(train[train['Survived'] == 1]['Embarked'].value_counts()/train['Embarked'].value_counts())
embarked_survival1.plot(linestyle='-', marker='o', ax=axes[0])
embarked_survival2 = pd.DataFrame({'Survived':train[train['Survived'] == 1]['Embarked'].value_counts(),
                                  'Died':train[train['Survived'] == 0]['Embarked'].value_counts()}, columns = ['Survived','Died'])
embarked_survival2[['Survived','Died']].plot(kind = 'bar', title='Male and Female Survival Comparison 1', alpha = 0.3, stacked = True, ax=axes[1])
</code></pre>

<p><img src="https://s3-us-west-1.amazonaws.com/sijunhe-blog/plots/post1/plot3.png" alt="png" /></p>

<p>I also generated two plots to check if different Port of Embarkation would make a difference in survival rate. It is surprising that it actually did have an impact. As passengers embarked from Cherbourg have a significant higher survival chance compared with passengers embarked from Queenstown or Southampton.</p>

<h2>Basic Models</h2>

<p>With the exploratory visualization from the last point, we have some idea what out data looks like. We know that the survival rate is in some way related to Pclass, Sex, Age, Fare and Embarked, but we haven&rsquo;t got any chance to explore the rest of the predicators. So, let&rsquo;s start building models and making predictions.</p>

<h3>Purely Gender-Based Model</h3>

<p>From last post, we know that the most influential predicator in the data set is Sex, as survival rate differs dramatically between male and female passengers. Given the low survival rate of male and high survival rate of female, the easiest possible model is to simply predict 0 for male and 1 for female.</p>

<pre><code class="python">#simply creating a Survival variable, mappped from the Sex variable
test['Survived'] = test['Sex'].map( {'female': 1, 'male': 0} ).astype(int)
</code></pre>

<p>The output of the head() function tells us our change to the data frame is effective and we have already made our first prediction! Next, we just need to export the data to a csv file and submit to Kaggle.</p>

<pre><code class="python">prediction_file = open("Gender_Model.csv", "wb")
out = csv.writer(prediction_file)
out.writerow(["PassengerId", "Survived"])
for i in range(0, len(test.PassengerId)):
    out.writerow([test.PassengerId[i], test.Survived[i]])
prediction_file.close()
</code></pre>

<p>The results of the gender-based prediction was <strong>0.76555</strong>, which is surprisingly good for a single line of prediction code! Of course we are not going to call it a day. Let&rsquo;s see if we could incorporate Pclass or Age in the model.</p>

<h3>Gender-Class Model</h3>

<p>While we could do a pivot table, predicting the majority of each gender in different Pclass, I&rsquo;d say it is time to bring in some models. Let&rsquo;s try our hands at <a href="https://en.wikipedia.org/wiki/Logistic_regression">Logistic Regression</a> first, since it is one of the most interpretable classification model in my opinion. We&rsquo;ll start with an extremely simple case of just 2 predicators: Pclass and Sex</p>

<pre><code class="python">from patsy import dmatrices
from sklearn.linear_model import LogisticRegression
#setting formula to predict Survived with Pclass and Sex
formula1 = 'Survived ~ C(Pclass) + C(Sex)'
train_response,train_data = dmatrices(formula1, data=train, return_type="dataframe")
test_response,test_data = dmatrices(formula1, data=test, return_type="dataframe")
LRmodel = LogisticRegression()
LRmodel.fit(train_data.values, train_response.values.ravel())
output = LRmodel.predict(test_data).astype(int)
</code></pre>

<p>It seems like we got the exact same result <strong>0.76555</strong> again. Why is that? Shouldn&rsquo;t an adding an predicator change some of our predictions? Here is why.</p>

<pre><code class="python">for i in range(0,2):
    for j in range(0,2):
        for k in range(0,2):
            if i == 1:
                Pclass = 2
            elif j == 1:
                Pclass = 3
            else: 
                Pclass = 1
            if i == j ==1:
                break    
            if k ==1:
                sex = "male"
            else:
                sex = "female"
            print "Given Pclass = %d and Sex = %s, predict %d for survival. " % (Pclass,sex, LRmodel.predict([[1,i,j,k]])) 
</code></pre>

<pre><code>Given Pclass = 1 and Sex = female, predict 1 for survival. 
Given Pclass = 1 and Sex = male, predict 0 for survival. 
Given Pclass = 3 and Sex = female, predict 1 for survival. 
Given Pclass = 3 and Sex = male, predict 0 for survival. 
Given Pclass = 2 and Sex = female, predict 1 for survival. 
Given Pclass = 2 and Sex = male, predict 0 for survival. 
</code></pre>

<pre><code class="python">print train.pivot_table('Survived', index=['Pclass','Sex'], aggfunc='mean')
</code></pre>

<pre><code>Pclass  Sex   
1       female    0.968085
        male      0.368852
2       female    0.921053
        male      0.157407
3       female    0.500000
        male      0.135447
Name: Survived, dtype: float64
</code></pre>

<p>Turns out that the predications stays the same, with or without the Pclass prediactor. Logistic Regression computes the probabilities for each response and predicts the response with largest likelihood. In this case, as we can see from the above pivot table, the most probable survival response for female is 1 and 0 for male, despite the change in survival rate due to Pclass, which is why the predictions stay the same.</p>

<p>Even though we didn&rsquo;t improve the model with the addition of the Pclass predicators, our effort was not totally worthless. Now we can add more variables to the Logistic Regression, hoping it would produce a more accurate prediction with the extra data. In the next post, we will explore some more complicated models.</p>
]]></content>
  </entry>
  
</feed>
